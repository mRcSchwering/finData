version: 1
jobs:
  build:
    docker:
      - image: python:3.5-slim
    steps:
      - checkout
      - run: echo $alphavantage_api_key
      - run: pip3 install -r requirements.txt
      - run: python -m unittest discover -s test -v
workflows:
  version: 1
  build:
    jobs:
      - build
