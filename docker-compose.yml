version: '2'

services:

  server:
    image: postgres:9.5.12-alpine
    expose:
      - '5432'

  client:
    build:
      context: _docker/psql-client/
    depends_on:
        - server

  app:
    image: mrcschwering/cci-findata_build:latest
    depends_on:
        - server
